# WOE-001: Flutter Project Scaffold

**ID:** WOE-001
**Phase:** 1 — Foundation
**Step:** 1.1
**Depends on:** —

---

## Description

Create the Flutter project from scratch and configure it as the foundation for all subsequent development. This includes the `pubspec.yaml` with every dependency listed in TECH_SPEC §5.1, lint rules, folder structure matching TECH_SPEC §6.1, and `--dart-define` environment variable wiring.

This is the first issue and must be completed before any other work begins. The goal is a `flutter run` that launches a blank app with no errors and `flutter analyze` that passes clean.

**Reference:** TECH_SPEC §6.1, §6.2, §6.6

---

## Definition of Done

- [ ] `flutter create` output cleaned up: remove default counter app, `test/widget_test.dart` replaced with a placeholder
- [ ] `pubspec.yaml` contains all dependencies from TECH_SPEC §5.1 at the specified versions
- [ ] `flutter_lints` configured in `analysis_options.yaml`; `flutter analyze` returns zero issues
- [ ] Folder structure under `lib/` matches TECH_SPEC §6.1 (empty placeholder files acceptable)
- [ ] `--dart-define` keys documented in a `.env.example` file at repo root
- [ ] `flutter pub get` completes without conflict
- [ ] `flutter test` passes (placeholder test only)
- [ ] `flutter build apk --debug` and `flutter build ios --no-codesign --debug` both succeed

---

## Sub-tasks

- [ ] Run `flutter create what_on_earth` with org `com.whatonearth`
- [ ] Replace generated `pubspec.yaml` with the full dependency list from TECH_SPEC §5.1 (flutter_inappwebview, sensors_plus, camera, drift, drift_flutter, shelf, shelf_static, shelf_router, dio, path_provider, path, flutter_secure_storage, supabase_flutter, flutter_riverpod, riverpod_annotation, uuid, intl, connectivity_plus, flutter_background_fetch)
- [ ] Add dev dependencies: drift_dev, build_runner, riverpod_generator, flutter_lints, mockito, build_verify
- [ ] Create `analysis_options.yaml` extending `package:flutter_lints/flutter.yaml`
- [ ] Create empty placeholder files for each module directory: `lib/position/`, `lib/sensors/`, `lib/globe/`, `lib/tile_cache/`, `lib/pins/`, `lib/onboarding/`, `lib/screens/`, `lib/shared/`
- [ ] Create `.env.example` listing all `--dart-define` variables: `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `ISS_POSITION_API`, `TLE_API`, `TILE_SERVER_PORT`, `CESIUM_LOCALHOST_PORT`
- [ ] Create `lib/main.dart` with a minimal `runApp` and `ProviderScope` (Riverpod root)
- [ ] Create `lib/app.dart` with a `MaterialApp` stub using a dark theme
- [ ] Verify `flutter analyze` is clean
- [ ] Verify both debug builds succeed
