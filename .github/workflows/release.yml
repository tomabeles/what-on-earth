name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build signed APK
        run: echo "TODO: configure signing and upload (WOE-056)"

      - name: Build signed IPA
        run: echo "TODO: configure signing and upload (WOE-056)"

      - name: Upload to distribution
        run: echo "TODO: configure signing and upload (WOE-056)"
